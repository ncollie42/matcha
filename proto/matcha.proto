syntax = "proto3";

package matcha;

service Person {
  rpc createUser (createRequest) returns (Reply);
  // rpc SayHelloAlot (HazukiRequest) returns (stream HelloReply);
  rpc getUsers (createRequest) returns(stream person);
  // create
  // delete
  // login
  // edditeProfile
  // getImage( for when on profile?)
  //   individual functions to eddit??
}

message person{
  int64 UID = 1;
  string firsName = 2;
  string lastName = 3;
  string password = 4;
  string email = 5;
  //gender [enum]
  //preference [enum]
  //bio
  //fame rating
  //array of recently view
  //Array of liked people
  //array of tags
  //location
  //pictures??
}

message createRequest{
  string userName = 1;
  string firsName = 2;
  string lastName = 3;
  string password = 4;
  string email = 5;
}

message Reply {
  string message = 1;
}